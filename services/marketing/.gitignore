# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Exclude generated css files (since we're using SASS)
src/**/*.css

# Explicitly keep the .env around to ensure NODE_PATH is always set
!.env

.eslintcache
.next
